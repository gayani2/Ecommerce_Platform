# Only needed when deploying to Choreo or any production host.
# Set this to your backend service's public URL in Choreo's environment variable settings.
# Leave this file empty (or don't create .env) for local development — the Vite proxy handles it.

VITE_API_BASE_URL=https://your-choreo-backend-url.choreo.dev
